// --- Root page — server component, imports CommandCenter ---
// SERVER component (no 'use client')

import CommandCenter from '@/components/layout/CommandCenter'

export default function Page() {
  return <CommandCenter />
}